const options = {
  strings: [
    '[Aspiring] Web developer',
    
  ],
  typeSpeed: 80,
  backSpeed: 50,
  backDelay: 1000,
  startDelay: 1000,
  loop: true,
};

const typed = new Typed('.animate-subheading', options);
